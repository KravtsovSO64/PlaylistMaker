<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/backgroundApp">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/favourite_track_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:visibility="gone"/>

    <FrameLayout
        android:id="@+id/is_empty_list_favourite"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/backgroundApp"
        android:visibility="visible">

        <ImageView
            android:id="@+id/mediaViewHolder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:src="@drawable/ic_not_found" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/ys_display_medium"
            android:textColor="@color/search_holder_trackName"
            android:textSize="19sp"
            android:layout_gravity="center"
            android:gravity="center"
            android:layout_marginTop="76dp"
            android:text="@string/mediaFavoriteHolder" />

    </FrameLayout>

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabViewMedia"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/backgroundApp"
        app:tabIndicatorColor="@color/setting_header_text"
        app:tabTextAppearance="@style/TextAppearance_TabWidget"
        app:tabSelectedTextColor="@color/setting_header_text"
        app:tabTextColor="@color/setting_header_text"/>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/pagerViewMedia"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"/>



</LinearLayout>






